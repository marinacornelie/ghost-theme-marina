<div class="card">
    <div class="border">
    <div class="card-header">
         <a href="{{url}}" class="card-header-title">{{title}}</a> 
    </div>
    <div class="card-image">
        <a href="{{url}}">
        <figure class="image is-4by3">
         <img src="{{feature_image}}" alt="Feature Image">  
        </figure>
        </a>
    </div>
    <div class="card-content"></div>
    </div>
</div>